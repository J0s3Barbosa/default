<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">MB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03"
        aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <!-- <li class="nav-item active">
                <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
            </li> -->
            <li class="nav-item">
                <a class="nav-link" href="/map">Map</a>
            </li>
            <% if(typeof user != 'undefined'){ %>
                <% if( user.name == 'Jose Barbosa'){ %>
                    <li class="nav-item">
                            <a class="nav-link" href="/users/ManageUsers">ManageUsers</a>
                        </li>
                   
                    <% } %>
                <% } %>
         
            <li class="nav-item">
                <a class="nav-link" href="/about">About</a>
            </li>
        </ul>

        <div class="topnav-right">


        
        <% if(typeof user != 'undefined'){ %>
        <form class="form-inline my-2 my-lg-0">
            <span class="form-control mr-sm-2">Welcome
                <%= user.name %></span>
            <a href="/users/logout" class="btn btn-secondary my-2 my-sm-0">Logout</a>
        </form>
        <% } %>
        <% if(typeof user == 'undefined'){ %>
            <form class="form-inline my-2 my-lg-0">
                <a href="/users/login" class="btn btn-secondary my-2 my-sm-0">Login</a>
            </form>

            <% } %>

        </div>

        
    </div>
</nav>

<style>
/* Add a black background color to the top navigation */
.topnav {
    background-color: #333;
    overflow: hidden;
}

/* Style the links inside the navigation bar */
.topnav a {
  float: left;
  color: #f2f2f2;
  text-align: center;
  padding: 14px 16px;
  text-decoration: none;
  font-size: 17px;
}

/* Change the color of links on hover */
.topnav a:hover {
  background-color: #ddd;
  color: black;
}

/* Add a color to the active/current link */
.topnav a.active {
  background-color: #4CAF50;
  color: white;
}

/* Right-aligned section inside the top navigation */
.topnav-right {
  float: right;
}


</style>